{
  "name": "sap-project",
  "version": "1.0.0",
  "description": "WashWizard Fiori Project",
  "license": "ISC",
  "author": "Rajat Patel",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "cds serve",
    "build:ui": "npx ui5 build --clean-dest",
    "build": "npx ui5 build --clean-dest && copy Staticfile dist\\",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.4.0",
    "@sap/cds-dk": "^9.9.0",
    "@ui5/cli": "^4.0.0",
    "sqlite3": "^6.0.1"
  },
  "dependencies": {
    "@cap-js-community/odata-v2-adapter": "^1.15.10",
    "@sap/cds": "^9.9.1",
    "express": "^5.2.1"
  },
  "cds": {
    "folders": {
      "app": "webapp"
    },
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "WashWizard.sqlite"
        }
      },
      "auth": {
        "kind": "dummy"
      }
    }
  }
}
